[1. ] == ![]   // true

Reason : 
// Why?

// ![] → false (because [] is truthy).

// Now it becomes [] == false.

// JS tries to convert [] to a number → 0.

// false also → 0.

// So 0 == 0 → ✅ true.


2. [] == ""
// 👉 Why?

// [] gets converted to string → "".

// "" == "" → ✅ true.


3. [] == 0;
// 👉 Why?

// [] becomes "".

// "" → 0 (when coerced to number).

// 0 == 0 → ✅ true.


4. "" == 0   // true
// Why?

// "" → 0 (numeric conversion).

// 0 == 0 → ✅ true.

5. null == undefined   // true
// 👉 Special rule in JS: null and undefined are only equal to each other when using ==.
// But:null === undefined   // true

6. NaN == NaN   // false
    // 👉 By definition, NaN is not equal to anything, even itself.
    // To check, use:

    // Number.isNaN(NaN)  // true


    ⚡ Summary of the madness:

// [] == ![] // true

// [] == "" // true

// [] == 0 // true

// "" == 0 // true

// "0" == false // true

// null == undefined // true

// NaN == NaN // false

// null == undefined   // true
// null == 0           // false
// undefined == 0      // false




// ✅ Step 3. One is Boolean

// If one side is boolean, convert it to a number:

// true → 1
// false → 0


// Example:

// true == 1   // true
// false == 0  // true
// "0" == false // true (because "0" → 0, false → 0)


// ✅ Step 4. One is String and other is Number

// Convert the string to number, then compare:

// "42" == 42   // true
// "0" == 0     // true
// "" == 0      // true


// ✅ Step 5. One is Object and other is Primitive

// If one is an object (array, function, etc.) and the other is a primitive (string/number/boolean):

// Convert the object to a primitive by calling valueOf() or toString().

// Examples:

// [] == ""     // true  ( [] → "" )
// [] == 0      // true  ( [] → "" → 0 )
// [1] == "1"   // true  ( [1] → "1" )




🔄 Flowchart for a == b
Start: a == b ?
   │
   ├── Same Type? ──► Compare like ===
   │                   │
   │                   └── true/false
   │
   ├── null / undefined combo?
   │         └── true
   │
   ├── One is String, other Number?
   │         └── Convert String → Number, compare
   │
   ├── One is Boolean?
   │         └── Convert Boolean → Number (true=1, false=0), compare
   │
   ├── One is Object, other Primitive?
   │         └── Convert Object → Primitive, compare again
   │
   └── Else
             └── false



